// tailwind.config.js

module.exports = {
    theme: {
      extend: {
        backgroundImage: theme => ({
          'cricket': "url('images/cricket-crop.png')",
        })
      }
    }
  }